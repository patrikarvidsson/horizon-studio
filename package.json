{
  "name": "horizon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf public; mkdir public; cp -Rv [m]* public/; nodemon -e js,mjs,jsx,json,tbtl,ndtl src/index.js",
    "stage": "rm -rf public; mkdir public; node src/index.js; cp -Rv [m]* public/; now",
    "build": "rm -rf public; mkdir public; node src/index.js; cp -Rv [m]* public/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git@git.sr.ht:~patrik/horizon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "csso": "^4.0.3"
  }
}
